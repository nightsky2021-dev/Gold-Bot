# 🧪 راهنمای تست ربات

## ✅ تغییرات اعمال شده (نسخه حرفه‌ای)

### 1. **رفع خطای دکمه‌ها**
- ✅ الگوهای Callback اصلاح شد
- ✅ مدیریت حالت‌های Conversation بهینه شد
- ✅ لاگ‌گذاری کامل اضافه شد
- ✅ مدیریت خطا بهبود یافت

### 2. **بهبودهای حرفه‌ای**
```python
# الگوهای دقیق برای callback handlers
SELECTING_PRODUCT: pattern='^trade_(gold|coin|dollar)$'
SELECTING_ACTION: pattern='^trade_(gold|coin|dollar)_action_(buy|sell)$'
SELECTING_METHOD: pattern='^(method_gram|method_rial)$'
CONFIRMING_TRADE: pattern='^(confirm_yes|confirm_no)$'
```

### 3. **مدیریت خطا**
- ✅ بررسی موجودی قبل از فروش
- ✅ Validation ورودی کاربر
- ✅ پیام‌های خطای واضح
- ✅ لاگ کامل برای debugging

---

## 📝 مراحل تست

### مرحله 1: راه‌اندازی اولیه

```bash
# در پوشه پروژه
cd C:\Gold_bot

# اطمینان از migrations
python manage.py makemigrations
python manage.py migrate

# ایجاد محصولات (اگر هنوز ندارید)
python setup_products.py

# بررسی محصولات
python manage.py shell
```

در shell:
```python
from trading.models import Product

# نمایش محصولات
for p in Product.objects.all():
    print(f"{p.product_code}: {p.name} - خرید: {p.buy_price:,} | فروش: {p.sell_price:,}")

exit()
```

### مرحله 2: اجرای ربات

```bash
# اجرای ربات با لاگ‌های کامل
python manage.py runbot
```

---

## 🧪 سناریوهای تست

### تست 1: ثبت‌نام کاربر جدید ✅

**مراحل:**
1. به ربات در تلگرام پیام دهید: `/start`
2. دکمه "📱 ارسال شماره تماس" را بزنید
3. شماره خود را به اشتراک بگذارید

**نتیجه مورد انتظار:**
```
✅ ثبت‌نام شما با موفقیت انجام شد!

⏳ حساب شما در انتظار تایید مدیر است.
```

---

### تست 2: تأیید کاربر ✅

**در ترمینال:**
```bash
python manage.py shell
```

```python
from users.models import Profile

# تأیید اولین کاربر
profile = Profile.objects.first()
profile.is_approved = True
profile.save()
print(f"✅ {profile.user.get_full_name()} تأیید شد")

exit()
```

**در تلگرام:**
- `/start` را دوباره بزنید
- باید منوی اصلی نمایش داده شود

---

### تست 3: مشاهده قیمت‌ها ✅

**مراحل:**
1. دکمه **📊 قیمت‌ها** را بزنید
2. **🪙 طلای آبشده** را انتخاب کنید

**نتیجه مورد انتظار:**
```
🪙 طلای آبشده (هر گرم)

💰 قیمت خرید از شما:
   3,200,000 ریال

💵 قیمت فروش به شما:
   3,500,000 ریال

[🟢 خرید] [🔴 فروش]
[🔙 بازگشت]
```

**چک کنید:**
- ✅ دکمه‌ها نمایش داده می‌شوند
- ✅ قیمت‌ها صحیح هستند
- ✅ دکمه بازگشت کار می‌کند

---

### تست 4: فرآیند خرید کامل ✅

**مراحل:**
1. دکمه **💎 معامله** را بزنید
2. **🪙 طلای آبشده** را انتخاب کنید
3. دکمه **🟢 خرید** را بزنید  ← **باید کار کند!**
4. **⚖️ مقدار (گرم/عدد)** را بزنید
5. عدد `2.5` را تایپ کنید
6. دکمه **✅ تایید و ثبت** را بزنید

**نتیجه مورد انتظار:**
```
✅ سفارش خرید با موفقیت ثبت شد!

🆔 شماره سفارش: 1
📦 محصول: طلای آبشده (هر گرم)
⚖️ مقدار: 2.5000 گرم
💰 مبلغ: 8,750,000 ریال

⏳ سفارش در انتظار تایید مدیر است.
```

---

### تست 5: فرآیند فروش ✅

**مراحل:**
1. دکمه **💎 معامله** را بزنید
2. **🪙 طلای آبشده** را انتخاب کنید
3. دکمه **🔴 فروش** را بزنید  ← **باید کار کند!**

**نتیجه مورد انتظار (اگر موجودی ندارید):**
```
❌ موجودی طلای شما صفر است.
ابتدا باید طلا خریداری کنید.
```

---

### تست 6: لغو معامله ✅

**مراحل:**
1. شروع یک معامله
2. در هر مرحله دکمه **❌ انصراف** را بزنید

**نتیجه مورد انتظار:**
```
❌ معامله لغو شد.
```

---

### تست 7: مشاهده پورتفولیو ✅

**مراحل:**
1. دکمه **👛 کیف پول** را بزنید

**نتیجه مورد انتظار:**
```
👛 کیف پول شما

💰 موجودی ریالی:
   0 ریال

⚖️ موجودی طلا:
   0.0000 گرم
```

---

### تست 8: مشاهده تاریخچه ✅

**مراحل:**
1. دکمه **📋 تاریخچه** را بزنید

**نتیجه مورد انتظار (اگر سفارشی دارید):**
```
📋 تاریخچه ۵ سفارش آخر

🟢 سفارش #1
   خرید از ما | طلای آبشده (هر گرم)
   مقدار: 2.5000 گرم
   مبلغ: 8,750,000 ریال
   ⏳ در انتظار بررسی
```

---

## 🐛 عیب‌یابی

### مشکل: دکمه‌های خرید/فروش کار نمی‌کنند

**بررسی لاگ:**
```
# در ترمینال که ربات اجرا می‌شود، باید این پیام‌ها را ببینید:
INFO - Callback received: trade_gold_action_buy
INFO - Action callback: trade_gold_action_buy
```

**اگر این پیام‌ها را نمی‌بینید:**
1. ربات را متوقف کنید (Ctrl+C)
2. دوباره اجرا کنید: `python manage.py runbot`
3. تست را تکرار کنید

---

### مشکل: خطای "محصول یافت نشد"

**راه حل:**
```bash
# بررسی محصولات
python manage.py shell
```

```python
from trading.models import Product

# نمایش تمام محصولات
products = Product.objects.all()
print(f"تعداد: {products.count()}")

for p in products:
    print(f"- کد: {p.product_code} | نام: {p.name} | فعال: {p.is_active}")

# اگر خالی است:
exit()
```

```bash
# ایجاد محصولات
python setup_products.py
```

---

### مشکل: قیمت‌ها صفر هستند

```bash
# به‌روزرسانی قیمت‌ها
python manage.py update_prices --show-details
```

**بررسی اتصال اینترنت:**
```bash
# تست اتصال به API
python -c "import requests; print(requests.get('http://api.navasan.tech/latest/?api_key=freeTET7c1g57cU7kPnjQa4KAMP7BWaS&item=abshodeh').text)"
```

---

## 📊 چک‌لیست تست نهایی

### عملکرد اصلی:
- [ ] ثبت‌نام کاربر جدید
- [ ] تأیید کاربر توسط ادمین
- [ ] نمایش منوی اصلی پس از تأیید
- [ ] نمایش قیمت‌ها (تک‌تک و همه)
- [ ] دکمه‌های خرید/فروش در صفحه قیمت کار می‌کنند

### معامله - خرید:
- [ ] شروع معامله
- [ ] انتخاب محصول
- [ ] دکمه خرید کار می‌کند ✅
- [ ] انتخاب واحد (گرم/ریال)
- [ ] وارد کردن مقدار
- [ ] نمایش پیش‌فاکتور
- [ ] تایید و ثبت سفارش

### معامله - فروش:
- [ ] دکمه فروش کار می‌کند ✅
- [ ] بررسی موجودی
- [ ] فرآیند کامل فروش

### سایر:
- [ ] لغو معامله در هر مرحله
- [ ] نمایش کیف پول
- [ ] نمایش تاریخچه
- [ ] بازگشت به منوی اصلی

---

## 🎯 نکات مهم

### 1. لاگ‌ها
همیشه ترمینالی که ربات در آن اجرا می‌شود را مشاهده کنید:
```
INFO - Callback received: trade_gold
INFO - Product code: gold
INFO - Action callback: trade_gold_action_buy
```

### 2. Cache
اگر تغییری اعمال کردید و کار نکرد:
- ربات را restart کنید
- کش تلگرام را پاک کنید (Settings > Data and Storage > Clear Cache)

### 3. Database
برای reset کامل (فقط در development):
```bash
# حذف دیتابیس
del db.sqlite3

# ایجاد مجدد
python manage.py migrate
python setup_products.py
```

---

## ✅ اگر همه چیز کار کرد

تبریک! 🎉 ربات شما آماده است:

1. **تولید (Production):**
   - توکن ربات واقعی را در `.env` قرار دهید
   - `DEBUG=False` تنظیم کنید
   - Server را راه‌اندازی کنید

2. **مانیتورینگ:**
   - لاگ‌ها را به فایل ذخیره کنید
   - خطاها را به Sentry یا مشابه آن ارسال کنید

3. **بهینه‌سازی:**
   - Cron job برای به‌روزرسانی قیمت‌ها
   - Backup خودکار دیتابیس
   - Rate limiting برای API

موفق باشید! 🚀

