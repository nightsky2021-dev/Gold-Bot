# 📚 راهنمای راه‌اندازی سریع - Quick Setup Guide

این راهنما شما را گام به گام در راه‌اندازی سیستم همراهی می‌کند.

## 🆕 ویژگی‌های جدید نسخه 1.1.0

### 💳 سیستم کیف پول چند ارزی
- پشتیبانی از چندین ارز: ریال، طلا، سکه، دلار
- مدیریت موجودی مسدود شده برای تراکنش‌های در انتظار
- سیستم واریز و برداشت با تایید ادمین

### 🏦 مدیریت حساب‌های بانکی
- افزودن حساب‌های بانکی با تایید ادمین
- پشتیبانی از شماره کارت 16 رقمی و شبا
- اعتبارسنجی نام صاحب حساب

### 📊 سیستم تراکنش‌ها
- تاریخچه کامل تراکنش‌ها
- شماره یکتای تراکنش
- آپلود رسید واریز
- مدیریت درخواست‌های برداشت

## ⚡ راه‌اندازی سریع (7 دقیقه)

### گام 1: نصب پکیج‌ها

```bash
# ساخت محیط مجازی
python3 -m venv venv
source venv/bin/activate  # در Windows: venv\Scripts\activate

# نصب requirements
pip install -r requirements.txt
```

### گام 2: تنظیم محیط

```bash
# کپی فایل .env
cp .env.example .env

# ویرایش .env و تنظیم توکن ربات
# حداقل باید TELEGRAM_BOT_TOKEN را تنظیم کنید
```

### گام 3: راه‌اندازی دیتابیس

```bash
# اجرای migrations
python manage.py migrate

# ساخت کاربر ادمین
python manage.py createsuperuser
```

### گام 4: ایجاد محصولات اولیه و تنظیم کیف پول

```bash
# اجرای سرور Django
python manage.py runserver
```

وارد پنل ادمین شوید: `http://localhost:8000/admin/`

#### محصولات را اضافه کنید:

| نام محصول | کد محصول | قیمت خرید | قیمت فروش | فعال |
|-----------|-----------|-----------|-----------|------|
| طلای آبشده | gold | 2500000 | 2550000 | ✅ |
| سکه تمام | coin | 15000000 | 15200000 | ✅ |
| دلار آمریکا | dollar | 420000 | 425000 | ✅ |

#### تنظیم کیف پول کاربران:
1. بخش **Users > Profiles** را باز کنید
2. پروفایل کاربر را ویرایش کنید
3. موجودی‌های مختلف را تنظیم کنید:
   - `Rial balance`: موجودی ریالی
   - `Gold balance`: موجودی طلا (گرم)
   - `Coin balance`: موجودی سکه (عدد)
   - `Dollar balance`: موجودی دلار
4. ذخیره کنید

#### مدیریت حساب‌های بانکی:
1. بخش **Users > Bank accounts** را باز کنید
2. حساب‌های بانکی کاربران را تایید کنید
3. فقط حساب‌های تایید شده قابل استفاده هستند

### گام 5: اجرای ربات

در یک ترمینال جدید:

```bash
python manage.py runbot
```

🎉 **تمام!** ربات شما آماده است.

## 🔧 تنظیمات پیشرفته

### استفاده از SQLite (پیش‌فرض)

سیستم به صورت پیش‌فرض از SQLite استفاده می‌کند که نیازی به نصب جداگانه ندارد:

```bash
# فایل دیتابیس به صورت خودکار در db.sqlite3 ایجاد می‌شود
# نیازی به تنظیمات اضافی نیست
```

### استفاده از PostgreSQL (اختیاری)

اگر می‌خواهید از PostgreSQL استفاده کنید:

```bash
# نصب PostgreSQL
sudo apt install postgresql postgresql-contrib

# ساخت دیتابیس
sudo -u postgres psql
CREATE DATABASE gold_shop;
CREATE USER gold_user WITH PASSWORD 'secure_password';
GRANT ALL PRIVILEGES ON DATABASE gold_shop TO gold_user;
\q

# تنظیم در .env
DATABASE_URL=postgres://gold_user:secure_password@localhost:5432/gold_shop

# نصب psycopg2
pip install psycopg2-binary>=2.9.9

# تغییر تنظیمات در settings.py
# DATABASES را به PostgreSQL تغییر دهید
```

### راه‌اندازی Cron Job برای به‌روزرسانی قیمت‌ها

```bash
# باز کردن crontab
crontab -e

# اضافه کردن job (هر ساعت)
0 * * * * cd /path/to/gold_shop && /path/to/venv/bin/python manage.py update_prices >> /var/log/gold_prices.log 2>&1
```

## 🧪 تست کردن سیستم

### 1. تست ربات در تلگرام

1. به ربات خود در تلگرام پیام `/start` بدهید
2. شماره تماس خود را ارسال کنید
3. در پنل ادمین، کاربر جدید را تایید کنید (`is_approved = True`)
4. دوباره `/start` بزنید - باید منوی اصلی را ببینید

### 2. تست خرید طلا

1. از منو "💰 خرید طلا" را انتخاب کنید
2. یک محصول را انتخاب کنید
3. روش محاسبه را انتخاب کنید
4. مقدار را وارد کنید (مثلا: 1.5)
5. تایید کنید

**نکته**: برای اینکه خرید موفق باشد، ابتدا باید موجودی ریالی کاربر را در پنل ادمین افزایش دهید.

### 3. تست سیستم کیف پول

#### افزایش موجودی تستی:
در پنل ادمین:
- بخش **Users > Profiles**
- پروفایل کاربر را باز کنید
- موجودی‌های مختلف را تنظیم کنید:
  - `Rial balance`: 10000000 (ده میلیون ریال)
  - `Gold balance`: 5.0 (پنج گرم طلا)
  - `Coin balance`: 2 (دو سکه)
  - `Dollar balance`: 100 (صد دلار)
- ذخیره کنید

#### تست واریز:
1. در ربات، منو "💳 کیف پول" را انتخاب کنید
2. "📥 واریز" را انتخاب کنید
3. ارز مورد نظر را انتخاب کنید
4. مبلغ را وارد کنید
5. رسید را آپلود کنید (اختیاری)
6. در پنل ادمین، تراکنش را تایید کنید

#### تست برداشت:
1. در ربات، منو "💳 کیف پول" را انتخاب کنید
2. "📤 برداشت" را انتخاب کنید
3. ارز مورد نظر را انتخاب کنید
4. مبلغ را وارد کنید
5. حساب بانکی مقصد را انتخاب کنید
6. در پنل ادمین، درخواست را تایید کنید

#### تست خرید و فروش:
حالا می‌توانید خرید و فروش طلا را تست کنید.

## 📋 Checklist راه‌اندازی

### پیش‌نیازها
- [ ] Python 3.9+ نصب شده
- [ ] محیط مجازی ساخته شده
- [ ] پکیج‌ها نصب شده
- [ ] فایل .env تنظیم شده
- [ ] توکن ربات از BotFather دریافت شده

### راه‌اندازی پایه
- [ ] Migrations اجرا شده
- [ ] کاربر ادمین ساخته شده
- [ ] محصولات اولیه اضافه شده (با کد محصول)
- [ ] ربات اجرا شده و پاسخ می‌دهد

### راه‌اندازی کیف پول
- [ ] موجودی‌های کاربران تنظیم شده
- [ ] حساب‌های بانکی کاربران تایید شده
- [ ] سیستم واریز تست شده
- [ ] سیستم برداشت تست شده
- [ ] تاریخچه تراکنش‌ها بررسی شده

### تست کامل
- [ ] خرید طلا تست شده
- [ ] فروش طلا تست شده
- [ ] مدیریت کیف پول تست شده
- [ ] پنل ادمین کامل بررسی شده

## 🐛 رفع مشکلات رایج

### مشکل: ربات پاسخ نمی‌دهد

**راه‌حل**:
1. بررسی کنید ربات (`python manage.py runbot`) در حال اجرا است
2. توکن را در `.env` چک کنید
3. لاگ‌ها را بررسی کنید

### مشکل: خطای Database

**راه‌حل**:
```bash
# پاک کردن migrations و دوباره اجرا
find . -path "*/migrations/*.py" -not -name "__init__.py" -delete
find . -path "*/migrations/*.pyc" -delete
python manage.py makemigrations
python manage.py migrate
```

### مشکل: "No module named '...'"

**راه‌حل**:
```bash
# مطمئن شوید محیط مجازی فعال است
source venv/bin/activate

# نصب مجدد requirements
pip install -r requirements.txt
```

### مشکل: کاربر تایید شده اما منو نمی‌بینید

**راه‌حل**:
دوباره دستور `/start` را در ربات بزنید.

### مشکل: خطای کیف پول

**راه‌حل**:
1. بررسی کنید موجودی کاربر کافی است
2. حساب‌های بانکی تایید شده‌اند
3. تراکنش‌های در انتظار را بررسی کنید

### مشکل: خطای حساب بانکی

**راه‌حل**:
```bash
# بررسی فرمت شماره کارت یا شبا
# شماره کارت باید 16 رقم باشد
# شبا باید با IR شروع شود و 24 رقم داشته باشد
```

## 📞 پشتیبانی

اگر مشکلی داشتید:

1. ابتدا لاگ‌ها را بررسی کنید
2. Documentation را مطالعه کنید
3. Issue باز کنید در GitHub (اگر وجود دارد)

---

**موفق باشید!** 🚀

