# 🚀 راهنمای سریع شروع (Quick Start)

این راهنما به شما کمک می‌کند در کمتر از 5 دقیقه پروژه را اجرا کنید.

## پیش‌نیازها

- Python 3.10+
- یک توکن ربات تلگرام از [@BotFather](https://t.me/botfather)

## مراحل نصب (3 دقیقه)

### 1. نصب پکیج‌ها
```bash
pip install -r requirements.txt
```

### 2. تنظیم متغیرهای محیطی
```bash
# کپی فایل .env.example
cp .env.example .env

# ویرایش .env و فقط TELEGRAM_BOT_TOKEN را تنظیم کنید
# توکن را از @BotFather دریافت کنید
nano .env
```

مقدار ضروری در `.env`:
```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### 3. راه‌اندازی دیتابیس
```bash
python manage.py migrate
```

### 4. ایجاد سوپریوزر
```bash
python manage.py createsuperuser
# نام کاربری: admin
# ایمیل: admin@example.com
# رمز عبور: (دلخواه)
```

### 5. افزودن داده‌های نمونه
```bash
python setup_sample_data.py
```

این اسکریپت:
- ✅ 5 محصول طلا ایجاد می‌کند
- ✅ یک کاربر تست با موجودی نمونه می‌سازد

## اجرای پروژه

### Terminal 1: سرور Django (پنل ادمین)
```bash
python manage.py runserver
```

پنل ادمین: http://localhost:8000/admin

### Terminal 2: ربات تلگرام
```bash
python manage.py runbot
```

## تست ربات

1. در تلگرام به ربات خود پیام `/start` بدهید
2. روی دکمه "ارسال شماره تماس" کلیک کنید
3. به پنل ادمین بروید: http://localhost:8000/admin
4. وارد بخش "پروفایل‌ها" شوید
5. کاربر جدید را تأیید کنید (is_approved ✓)
6. در ربات دوباره `/start` بزنید
7. حالا می‌توانید از منو استفاده کنید!

## دستورات مفید

```bash
# اجرای migrations
python manage.py makemigrations
python manage.py migrate

# ایجاد سوپریوزر
python manage.py createsuperuser

# اجرای ربات
python manage.py runbot

# به‌روزرسانی قیمت‌ها
python manage.py update_prices --dry-run  # نمایش بدون تغییر
python manage.py update_prices             # اعمال تغییرات

# Shell Django
python manage.py shell

# اجرای سرور
python manage.py runserver

# افزودن داده نمونه
python setup_sample_data.py
```

## منوی اصلی ربات

پس از تأیید، کاربر این منو را می‌بیند:

```
📈 قیمت لحظه‌ای     💰 خرید طلا
🛒 فروش طلا         📊 پورتفولیو من
📜 تاریخچه سفارشات
```

## فرآیند خرید/فروش

1. کاربر روی "خرید طلا" یا "فروش طلا" کلیک می‌کند
2. محصول را انتخاب می‌کند
3. روش محاسبه را انتخاب می‌کند:
   - ⚖️ بر اساس مقدار (گرم)
   - 💰 بر اساس مبلغ (ریال)
4. مقدار را وارد می‌کند
5. پیش‌فاکتور را تأیید می‌کند
6. سفارش ثبت می‌شود (وضعیت: در انتظار)
7. مدیر در پنل ادمین سفارش را بررسی و تأیید می‌کند
8. موجودی کاربر به صورت خودکار به‌روزرسانی می‌شود

## پنل ادمین

### تأیید کاربران
1. به `پروفایل‌ها` بروید
2. کاربران در انتظار را انتخاب کنید
3. از dropdown action ها، "تأیید کاربران انتخاب شده" را بزنید
4. GO کلیک کنید

### مدیریت محصولات
1. به `محصولات` بروید
2. می‌توانید مستقیم از لیست قیمت‌ها را ویرایش کنید
3. یا روی محصول کلیک کنید برای جزئیات بیشتر

### پردازش سفارشات
1. به `سفارشات` بروید
2. فیلتر کنید بر اساس وضعیت: "در انتظار بررسی"
3. سفارشات را انتخاب کنید
4. از dropdown: "تکمیل سفارشات انتخاب شده"
5. موجودی‌ها خودکار به‌روزرسانی می‌شوند!

## رفع مشکلات رایج

### ربات پاسخ نمی‌دهد
- ✅ بررسی کنید TELEGRAM_BOT_TOKEN درست است
- ✅ مطمئن شوید `python manage.py runbot` در حال اجراست
- ✅ لاگ‌ها را بررسی کنید

### کاربر نمی‌تواند سفارش ثبت کند
- ✅ بررسی کنید is_approved = True
- ✅ مطمئن شوید حداقل یک محصول is_active = True دارد

### خطای Database
- ✅ اجرای `python manage.py migrate`
- ✅ اطمینان از وجود فایل db.sqlite3

## مرحله بعدی

- 📖 [README.md](README.md) کامل را بخوانید
- 🔧 قیمت‌های محصولات را در پنل ادمین تنظیم کنید
- 🔄 یک Cron Job برای `update_prices` تنظیم کنید
- 🚀 برای production از PostgreSQL استفاده کنید
- 🔒 DEBUG=False و SECRET_KEY را تغییر دهید

## پشتیبانی

سوال دارید؟ یک Issue در GitHub باز کنید!

---

✨ **موفق باشید!** ✨
