# 🚀 راهنمای شروع سریع

## نصب و راه‌اندازی در 5 دقیقه

### 1️⃣ کلون و نصب وابستگی‌ها

```bash
# ورود به دایرکتوری پروژه
cd /path/to/gold_shop

# نصب وابستگی‌ها
pip install -r requirements.txt
```

یا استفاده از اسکریپت خودکار:

```bash
./setup.sh
```

### 2️⃣ تنظیم توکن ربات تلگرام

1. به [@BotFather](https://t.me/botfather) در تلگرام بروید
2. دستور `/newbot` را بزنید
3. نام و username برای ربات انتخاب کنید
4. توکن دریافتی را کپی کنید

فایل `.env` را ویرایش کنید:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### 3️⃣ ایجاد سوپریوزر (ادمین)

```bash
python manage.py createsuperuser
```

اطلاعات زیر را وارد کنید:
- Username
- Email (اختیاری)
- Password

### 4️⃣ اجرای سرور و ربات

**ترمینال 1 - سرور Django:**
```bash
python manage.py runserver
```

**ترمینال 2 - ربات تلگرام:**
```bash
python manage.py runbot
```

### 5️⃣ شروع استفاده

1. **پنل ادمین**: [http://localhost:8000/admin/](http://localhost:8000/admin/)
   - ورود با username و password سوپریوزر
   - تایید کاربران جدید
   - مدیریت محصولات و سفارشات

2. **ربات تلگرام**:
   - ربات خود را در تلگرام پیدا کنید
   - دستور `/start` بزنید
   - شماره تماس خود را به اشتراک بگذارید
   - منتظر تایید ادمین باشید

## 🔧 تنظیمات پیشرفته

### استفاده از PostgreSQL

```env
DATABASE_URL=postgres://username:password@localhost:5432/gold_shop_db
```

### تنظیم Cron Job برای به‌روزرسانی قیمت‌ها

```bash
crontab -e
```

افزودن خط زیر برای به‌روزرسانی هر 30 دقیقه:
```
*/30 * * * * cd /path/to/gold_shop && python manage.py update_prices
```

## 🎯 گردش کار معمول

### برای ادمین:
1. ورود به پنل ادمین
2. تایید کاربران جدید: `Users → Profiles`
3. به‌روزرسانی قیمت محصولات: `Trading → Products`
4. بررسی و تایید سفارشات: `Trading → Orders`

### برای کاربر:
1. ثبت‌نام در ربات: `/start`
2. انتظار تایید ادمین
3. مشاهده قیمت‌ها: 📈 قیمت لحظه‌ای
4. خرید یا فروش طلا
5. مشاهده موجودی: 📊 پورتفولیو من
6. مشاهده تاریخچه: 📜 تاریخچه سفارشات

## 🐛 رفع مشکلات رایج

### ربات پاسخ نمی‌دهد
- مطمئن شوید توکن ربات صحیح است
- بررسی کنید `python manage.py runbot` در حال اجراست
- لاگ‌ها را بررسی کنید

### کاربر نمی‌تواند سفارش ثبت کند
- مطمئن شوید کاربر توسط ادمین تایید شده است
- بررسی کنید محصولات فعال هستند

### خطای دیتابیس
```bash
# حذف دیتابیس و شروع مجدد
rm db.sqlite3
python manage.py migrate
python init_sample_data.py
```

## 📚 مستندات بیشتر

برای اطلاعات کامل، [README.md](README.md) را مطالعه کنید.

## 💡 نکات مهم

- ✅ همیشه فایل `.env` را در `.gitignore` نگه دارید
- ✅ برای پروداکشن از PostgreSQL استفاده کنید
- ✅ `SECRET_KEY` را در پروداکشن تغییر دهید
- ✅ `DEBUG=False` در پروداکشن تنظیم کنید
- ✅ از HTTPS برای پنل ادمین استفاده کنید

---

**موفق باشید! 🎉**
